{% extends'base.html.twig' %}

{% block body %}
<div class="jumbotron">
    <h1>Détail produit</h1>
        <div class="row">
                <div class="col-md-6"><img src="{{produit.image}}" alt=""></div>
                <div class="col-md-6">
                     <p>Nom:{{produit.nom}}</p>
                     <p>Produit:{{produit.marque}}</p>
                     <p>Prix:{{produit.prix}}€</p>
                </div>
                </div>
                <h1>Description</h1>
                <p>{{ produit.description }}</p>
</div>

{% endblock  %}